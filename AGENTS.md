# Website delivery preference

Prepare and verify all requested website changes locally first. Before pushing to GitHub or deploying backend changes, show the concrete completed result and ask the user: “GitHub-a tətbiq edim?” Wait for their approval. This preference applies to future website edits as well. Do not ask again for a release the user has already approved.

## Render intake workflow
The owner-designated source folder is `E:/Mirzazada Studio WEB/Mirzazadastudio.com`.
Future new projects will be placed in its `New` subfolder. On request, import these into the website's general project catalog, prepare responsive images, and show the completed local preview. After a successful approved publication, move processed project folders from New to the general source folder, preserving originals and avoiding overwrites. Verify absolute source/destination paths before moving; ask only if a naming collision needs a user decision. Do not set up background monitoring unless requested.
Inspect every image's bottom edge. Crop any visible footer logo just enough to remove it from all website derivatives, including full-size and responsive copies. Keep source originals unchanged.
